- `VolcengineSTTService` requests second-pass recognition, without which the provider closes no segments on a long-lived connection and the service emits interim results only. Committed segments are now identified by their end timestamp, because second-pass segments carry no start timestamp.
