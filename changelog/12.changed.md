- `local-voice-app` configures Volcengine with one shared `VOLCENGINE_API_KEY` for recognition and synthesis, and fixes the model versions in code instead of reading them from `VOLCENGINE_RESOURCE_ID` and `VOLCENGINE_TTS_RESOURCE_ID`. `VOLCENGINE_TTS_API_KEY` is no longer used. Setting any of the three retired variables fails session creation with the replacement named.
