# Backend configuration for both local-voice-app entry points.
# Copy values into the repository root .env, not this directory.

# Recognition provider: elevenlabs (default) or volcengine.
STT_PROVIDER=elevenlabs

# ElevenLabs speech synthesis is required with either recognition provider.
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=

# DeepSeek language model.
DEEPSEEK_API_KEY=

# Required only when STT_PROVIDER=volcengine.
VOLCENGINE_API_KEY=
VOLCENGINE_RESOURCE_ID=volc.seedasr.sauc.duration

# Optional JSON: enable_itn, enable_punc, and provider corpus_context.
# Omitted flags use provider defaults. Second-pass recognition is disabled.
VOLCENGINE_STT_OPTIONS={}
# VOLCENGINE_STT_OPTIONS='{"enable_itn":true,"enable_punc":true,"corpus_context":{"hotwords":[{"word":"Pipecat"}]}}'
